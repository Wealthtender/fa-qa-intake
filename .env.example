# ---- Required ----
ANTHROPIC_API_KEY=sk-ant-xxxxxxxx

# ---- Optional (sensible defaults) ----
ANTHROPIC_MODEL=claude-sonnet-4-20250514
PORT=3000

# ---- Optional: live article config from Airtable ----
# Without these, the app serves the built-in SpaceX default config.
AIRTABLE_API_KEY=patxxxxxxxx
AIRTABLE_BASE_ID=apppicofJgY9YMx7G
AIRTABLE_ARTICLES_TABLE=Articles

# ---- Optional: where completed submissions are sent ----
# This is the Zapier webhook that triggers the editorial pipeline.
# Without it, submissions are logged to the server console.
ZAPIER_WEBHOOK_URL=https://hooks.zapier.com/hooks/catch/xxxxx/yyyyy/
